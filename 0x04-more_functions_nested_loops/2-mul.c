#include "main.h"
/**
 * _isdigit - print digit from 0 to 9
 * @c: contains compared value
 * Return: 0 success
 */
int _isdigit(int c)
{
	if (c = 0; c <= 9; c++)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
