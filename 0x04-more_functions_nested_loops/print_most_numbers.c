#include "main.h"

/**
 * print_most_numbers - prints number expect 2 and 4
 *
 * Reutrn: 0 successs
 */
void print_most_numbers(void)
{
	int i;

	for (i = 20;x < 30; x++)
	{
		if(i != 22 && i != 24)
		{
			_putchar(x);
		}
	_putchar(10);
	return (0);
	}
}
