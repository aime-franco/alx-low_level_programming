#include "main.h"
#include <stdio.h>
/**
 * _isupper -check the upper case alphabet
 *
 * Return: 0 success
 */
int _isupper(int c)
{
	char upper;
	
	c = 'C';
	for (upper = 'A'; uper <= 'Z'; upper++)
	{
	 	if (c != 'C')
		       return (1);
	}
		else
		{
			return (0);
		}
}
