#include "main.h"

/**
 * print_most_numbers - prints number expect 2 and 4
 *
 * Reutrn: 0 successs
 */
void print_most_numbers(void)
{
	int i;

	for (i = 48;x < 58; x++)
	{
		if(i != 50 && i != 52)
		{
			_putchar(x);
		}
	_putchar(10);
	return (0);
	}
}
