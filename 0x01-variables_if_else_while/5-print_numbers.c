#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main - Entry point
 *
 *
 * Return: 0 after excuting the code
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d",num);
	printf("\n");
	return (0);
}
