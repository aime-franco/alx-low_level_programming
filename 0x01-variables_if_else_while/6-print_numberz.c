#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: 0 after excuting codde
 */
int main(void)
{
	int nums;

	for (nums = '0'; nums < '10'; nums++)
		putchar(nums);
	putchar('\n');
	return (0);
}
