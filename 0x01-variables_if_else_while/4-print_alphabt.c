#include <stdlib.h>
#include <stdio.h>
#include <time.h>
int main(void)
{
	char low;
	e = 'e';
	q = 'q';

	for (low = 'a'; low = 'z'; low++)
	{
		if (low != e && low != q)
			putchar(low);
	}
	puchar('\n');
	return (0);
}
