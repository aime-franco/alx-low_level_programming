#include "main.h"
#include <stdio.h>

/**
 * print_array - prins the element of the array
 * @a: contains compared value of a
 * @n: contains compared value of n
 */
void print_array(int *a, int n)
{
	int x;

	for (x = 0; x < n; x++ )
	{
		printf("%d", i);
		if (x < n - 1)
		{
			printf(", ");
		}
	}
	prinf("\n");
}
